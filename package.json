{
  "name": "@better-slop/ocx",
  "private": true,
  "workspaces": [
    "packages/*",
    "internals/*"
  ],
  "catalog": {
    "@opencode-ai/plugin": "latest",
    "typescript": "5.9.3"
  },
  "scripts": {
    "compile:scripts": "bun build scripts/* --compile --out dist/scripts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "catalog:*",
    "@better-slop-internals/tsconfig": "workspace:*"
  }
}
